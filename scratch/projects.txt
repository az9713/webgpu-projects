[1] C1 Audio, signals and music :: 1 · Convolution reverb — put a voice inside a cathedral You drop a dry vocal and an impulse response of a real room. The page plays the voice back inside that room. The maths is fast convolution: transform both signals, multiply them as complex numbers, transform back. ai.onnx.DFT ai.onnx.Mul ai.onnx.Sub ai.onnx.Add Ranks here for surprise and significance — this is a professional audio tool, and it is four kernel calls. Ceiling — a long impulse response needs block convolution, so split the signal and overlap-add the pieces.

[2] C1 Audio, signals and music :: 2 · Phase vocoder — slow speech down without lowering the pitch A slider takes the playback to half speed. The voice stays at the same pitch. You watch the spectrogram stretch while the horizontal bands stay level. ai.onnx.HannWindow ai.onnx.STFT ai.onnx.Atan ai.onnx.Sin ai.onnx.Cos ai.onnx.DFT Ranks here for significance — this is the algorithm behind every time-stretch in every audio editor. Ceiling — phase unwrapping between frames is sequential, so keep that step in JavaScript over GPU-resident data.

[3] C1 Audio, signals and music :: 3 · Audio fingerprint matcher — the Shazam trick, visible Two files go in. The page finds spectrogram peaks in both, draws the constellation of peaks, and shows the matching pairs as lines between the two images. ai.onnx.STFT ai.onnx.MaxPool ai.onnx.Equal ai.onnx.NonZero ai.onnx.TopK Ranks here for surprise — peak picking is a max-pool followed by an equality test, which almost nobody expects. Ceiling — hash lookup is a JavaScript map, not a kernel.

[4] C1 Audio, signals and music :: 4 · Live spectrogram waterfall You speak, or you drop a song. A scrolling colour waterfall shows the frequency content. Every frame costs two kernel calls and no JavaScript arithmetic. ai.onnx.HannWindow ai.onnx.STFT ai.onnx.Mul ai.onnx.Sqrt ai.onnx.Log Ranks here for visualization — it is the clearest first build in this whole document. Ceiling — audio frames are small, so the GPU will idle. The value is the missing library, not the speed.

[5] C1 Audio, signals and music :: 5 · Spectral denoiser with a visible noise profile You mark two seconds of silence. The page learns the noise spectrum, subtracts it from every frame, and plays the clean audio. A second waterfall shows exactly what was removed. ai.onnx.STFT ai.onnx.ReduceMean ai.onnx.Sub ai.onnx.Clip ai.onnx.DFT Ranks here for visualization — showing the removed noise as its own image is more convincing than any before-and-after pair. Ceiling — aggressive subtraction adds musical artefacts. That is the algorithm, not the kernel.

[6] C1 Audio, signals and music :: 6 · Pitch tracker by cepstrum You sing. The page names the note. The trick is a Fourier transform of the logarithm of a Fourier transform, which turns a harmonic stack into a single spike. ai.onnx.DFT ai.onnx.Log ai.onnx.Abs ai.onnx.ArgMax Ranks here for surprise — the cepstrum is a beautiful idea and it is two DFT calls. Ceiling — low male voices need a long window, which adds latency.

[7] C1 Audio, signals and music :: 7 · Window function explorer Three windows, three spectra, side by side. You see why a rectangular window smears a pure tone and why a Blackman window does not. ai.onnx.HannWindow ai.onnx.HammingWindow ai.onnx.BlackmanWindow ai.onnx.DFT Ranks here for teaching value — spectral leakage is hard to explain and easy to show. Ceiling — it is a static page. There is no ambition in it beyond clarity.

[8] C2 Images and pixels :: 1 · Seam carving — the photo shrinks and the subject does not You drag a slider. The image narrows. A person in the frame stays the correct shape, because the page removes a wandering low-energy path of pixels instead of scaling. ai.onnx.Conv ai.onnx.Abs ai.onnx.Add ai.onnx.Min ai.onnx.ArgMin ai.onnx.Slice ai.onnx.Concat Ranks here for visualization and surprise — the effect looks impossible the first time you see it. Ceiling — the dynamic program runs one row at a time, so it is one dispatch per row. A tall image costs many small dispatches.

[9] C2 Images and pixels :: 2 · Seamless clone — paste a face into another photo with no visible edge You drop two images and draw a region. The page pastes the region and the seam disappears, because it copies gradients rather than pixels and then solves for the result. ai.onnx.Conv ai.onnx.Add ai.onnx.Mul ai.onnx.Where Ranks here for significance — Poisson blending is the core of every modern healing brush. Ceiling — there is no linear solver in the 207, so this is Jacobi iteration with a 3×3 stencil. Hundreds of iterations, each one cheap.

[10] C2 Images and pixels :: 3 · Warp studio — drag the corners, bend the picture Perspective correction, ripple, swirl and pinch, applied live while you drag. The manifest for the sampler confirms rank-4 NCHW input with linear, nearest and cubic interpolation. ai.onnx.AffineGrid ai.onnx.GridSample ai.onnx.Resize Ranks here for visualization — a warp is instant feedback, and cubic sampling looks professional. Ceiling — the flow field for a non-affine warp is built in JavaScript once, then re-used.

[11] C2 Images and pixels :: 4 · Convolution unmasked — im2col, one matrix multiply, and back Two panels compute the same blurred image. The left one calls the convolution kernel. The right one flattens the patches, does a single matrix multiply, and folds the result back. The pixels match exactly. ai.onnx.Conv ai.onnx.MatMul ai.onnx.Col2Im ai.onnx.Sub ai.onnx.Abs Ranks here for significance — this identity is why GPUs run deep learning at all, and almost every explanation of it is a diagram rather than a running program. Ceiling — the im2col expansion uses a lot of memory. Keep the image small.

[12] C2 Images and pixels :: 5 · Sub-pixel upscaling with a channel shuffle A small image becomes a large one. The extra detail comes out of the channel dimension, not from interpolation. Two clicks switch between the shuffle and a plain resize, so you can see the difference. ai.onnx.DepthToSpace ai.onnx.SpaceToDepth ai.onnx.Conv ai.onnx.Resize Ranks here for surprise — DepthToSpace is exactly the pixel-shuffle layer of every super-resolution network, and it is one call. Ceiling — without trained weights the result is a demonstration, not a real upscaler. Add a small model to make it one.

[13] C2 Images and pixels :: 6 · Filter rack with an editable matrix A 3×3 grid of numbers you can type into. Edge detection, sharpen, emboss and blur appear as you type. Presets sit under the grid. ai.onnx.Conv com.microsoft.FusedConv ms ai.onnx.Clip ai.onnx.Pad Ranks here for teaching value — the fused variant also lets you compare a fused activation against a separate one. Ceiling — it is the classic demo. Its worth is as a base for the harder projects above.

[14] C2 Images and pixels :: 7 · Pooling and unpooling, made visible The page pools an image down, then unpools it back. The unpooled image is sparse: only the winning pixels survive. You are looking at exactly which pixels a network keeps. ai.onnx.MaxPool ai.onnx.MaxUnpool ai.onnx.AveragePool ai.onnx.LpPool ai.onnx.LRN Ranks here for surprise — MaxUnpool and LRN are obscure kernels and this is the one page that makes them obvious. Ceiling — small teaching artifact. No further ambition.

[15] C3 Physics and simulation :: 1 · Real-time fluid you can stir with the mouse Smoke and dye swirl across a grid. You drag and the flow follows your hand. Movement uses backward tracing through the flow field, and pressure uses repeated stencil passes. ai.onnx.GridSample ai.onnx.Conv ai.onnx.Add ai.onnx.Sub ai.onnx.Mul ai.onnx.Div Ranks here for visualization and surprise — the semi-Lagrangian advection step of a stable-fluids solver is GridSample , exactly, with no adaptation. Ceiling — the pressure solve wants 20 to 40 Jacobi iterations per frame. Each is one dispatch, so watch the frame budget.

[16] C3 Physics and simulation :: 2 · A quantum wavepacket tunnelling through a barrier You draw a potential barrier with the mouse. A wavepacket runs into it. Part of it bounces and part of it appears on the far side. The colour shows the phase. ai.onnx.DFT ai.onnx.Mul ai.onnx.Exp ai.onnx.Sin ai.onnx.Cos ai.onnx.Add Ranks here for surprise and significance — the split-step method alternates a multiply in position space with a multiply in frequency space, so it is a DFT sandwich. Real quantum mechanics, in a browser tab, with no library. Ceiling — a 512×512 complex grid is comfortable. Larger grids need a batched two-dimensional transform.

[17] C3 Physics and simulation :: 3 · Reaction and diffusion — Turing patterns you can paint into Spots, stripes and coral shapes grow across a million-cell grid. You paint chemical into it and watch the pattern reorganise around your stroke. ai.onnx.Conv ai.onnx.Mul ai.onnx.Sub ai.onnx.Add ai.onnx.Clip Ranks here for visualization — it is the most beautiful output per line of code in this document. Ceiling — the two feed and kill parameters decide everything. Put them on sliders or the page looks dead.

[18] C3 Physics and simulation :: 4 · Ripple tank with a double slit Waves cross a barrier with two openings. Interference fringes form on the far side. You drag the slit spacing and watch the fringes move. ai.onnx.Conv ai.onnx.Add ai.onnx.Mul ai.onnx.Where ai.onnx.Clip Ranks here for significance — this is the most famous experiment in physics and it is a five-point stencil. Ceiling — absorbing boundaries need a damping ring, or reflections ruin the picture.

[19] C3 Physics and simulation :: 5 · N-body galaxy Four thousand stars pull on each other. Arms form. You seed the disc and watch it wind up. ai.onnx.Sub ai.onnx.Mul ai.onnx.ReduceSum ai.onnx.Pow ai.onnx.Einsum Ranks here for significance — all-pairs force is an N×N problem, which is the exact shape the GPU is best at. Ceiling — memory grows with the square of the body count. Four thousand bodies is 16 million pairs and is comfortable. Forty thousand is not.

[20] C3 Physics and simulation :: 6 · Cloth on a lattice A sheet hangs from two corners and swings. You cut it with the mouse and it tears. ai.onnx.Conv ai.onnx.Sub ai.onnx.Mul ai.onnx.Add ai.onnx.Where Ranks here for visualization — spring forces on a regular grid are a stencil, so the whole sheet updates in one call. Ceiling — a tear breaks the regular grid, so handle cuts as a mask rather than as a change of topology.

[21] C3 Physics and simulation :: 7 · Heat equation sandbox You paint hot and cold regions and press play. Heat spreads. Isotherms appear as contour lines. ai.onnx.Conv ai.onnx.Add ai.onnx.Mul ai.onnx.Clip Ranks here for teaching value — it is the simplest correct simulation in this list and a good first build. Ceiling — the explicit scheme goes unstable if the time step is too large. Keep the step small or use more iterations.

[22] C4 Generative art :: 1 · An untrained neural network as a painting machine Feed every pixel coordinate into a small random network. Read the three outputs as colour. The result is a smooth, organic, high-resolution image that no person designed. Re-roll the weights and get a completely different one. ai.onnx.MatMul ai.onnx.Tanh ai.onnx.Sin ai.onnx.Sigmoid ai.onnx.Concat Ranks here for surprise — it is a neural network that was never trained, and it produces art. It is also the cheapest possible use of a matrix multiply kernel. Ceiling — at 1024×1024 the first layer is a million-row matrix multiply. It is a thin, wide shape, so it does far less arithmetic per row than the one measured number here, which is 9.3 ms for a 1024 square multiply.

[23] C4 Generative art :: 2 · Flow-field particle painting A million particles drift along an invisible vector field and leave trails. The field bends as you move the mouse. ai.onnx.GridSample ai.onnx.Add ai.onnx.Mul ai.onnx.ScatterND Ranks here for visualization — sampling a field at a million arbitrary positions is one kernel call. Ceiling — the scatter into the accumulation image can collide. Accept the small error or accumulate in several passes.

[24] C4 Generative art :: 3 · Strange attractor density maps Lorenz, Clifford and De Jong attractors, drawn as a density image from a million iterated points. Fine filament structure appears that a line drawing never shows. ai.onnx.Sin ai.onnx.Cos ai.onnx.Mul ai.onnx.Add ai.onnx.ScatterND ai.onnx.Log Ranks here for visualization — density accumulation is what makes these look like photographs instead of scribble. Ceiling — each iteration is a dispatch, so run a million points for 200 steps rather than one point for 200 million.

[25] C4 Generative art :: 4 · Domain-warped noise landscape Clouds, marble, wood grain and terrain, all from one noise texture pushed through itself twice. Sliders control the warp strength. ai.onnx.GridSample ai.onnx.Mul ai.onnx.Add ai.onnx.Resize ai.onnx.Clip Ranks here for surprise — domain warping is famous among shader writers and is three sampling calls here. Ceiling — the base noise comes from JavaScript, because no kernel generates random numbers.

[26] C4 Generative art :: 5 · Deep zoom into the Mandelbrot set You click and the view dives. The iteration count rises as you go deeper and the boundary keeps producing new structure. ai.onnx.Mul ai.onnx.Add ai.onnx.Sub ai.onnx.Where ai.onnx.ReduceSum Ranks here for visualization — a classic, and a clean measure of raw throughput. Ceiling — single precision runs out at around 10 -7 zoom. Deeper needs double-double arithmetic, which these kernels do not have.

[27] C4 Generative art :: 6 · Truchet tiles and quasi-periodic patterns A grid of tiles, each rotated by a rule. Curves join across tile edges and form long continuous paths. ai.onnx.Mod ai.onnx.Where ai.onnx.Floor ai.onnx.GridSample Ranks here for visualization — simple rules, complex result, and it renders instantly at any size. Ceiling — the tile choice needs a random value per cell, which again comes from JavaScript.

[28] C4 Generative art :: 7 · Checkerboard artefacts as a subject Transposed convolution upsamples a small image and leaves a regular grid pattern. The page turns that famous flaw into a controllable texture generator. ai.onnx.ConvTranspose ai.onnx.Resize ai.onnx.Sub Ranks here for surprise — it explains a real artefact that ruined many generative models, by letting you produce it deliberately. Ceiling — narrow subject. Best as one panel inside a larger page.

[29] C5 Transformer internals :: 1 · One full decoder layer, real weights, every head visible You type a sentence. The page runs a complete Llama-style block on your GPU and draws each attention head as a heat map. Nothing leaves the machine. ai.onnx.RMSNormalization ai.onnx.RotaryEmbedding ai.onnx.Attention ai.onnx.MatMul ai.onnx.SwiGLU ai.onnx.Add Ranks here for all three axes — it is the best-looking, most significant and least expected thing on this list. The launch demo showed one mechanism in twenty lines. This is the whole layer. Ceiling — the gate and value inputs to SwiGLU must have identical shapes, because the kernel does not broadcast.

[30] C5 Transformer internals :: 2 · Head atlas — find the induction heads yourself Every head of every layer, as a grid of small heat maps. Click one to enlarge it. Feed a repeated sequence and watch specific heads light up on the repeat. ai.onnx.Attention com.microsoft.MultiHeadAttention ms ai.onnx.Softmax ai.onnx.TopK Ranks here for significance — induction heads are a genuine research finding, and this makes them reproducible on a laptop. Ceiling — hundreds of heat maps at once needs one canvas and manual tiling, not hundreds of elements.

[31] C5 Transformer internals :: 3 · Logit lens — read the model's mind at every layer hybrid Take the hidden state after each layer, apply the final norm and the output matrix, and show the top five tokens. You watch the prediction form, layer by layer. ai.onnx.LayerNormalization ai.onnx.MatMul ai.onnx.TopK ai.onnx.Softmax Ranks here for surprise — the answer is often decided several layers before the end, and seeing that happen is memorable. Ceiling — needs hidden states from a real model, so run the model with Transformers.js and do the lens with kernels.

[32] C5 Transformer internals :: 4 · Rotary position embedding, drawn as rotation Each pair of dimensions is a point on a circle. As the token position advances, the point rotates. Two tokens at any fixed distance always keep the same angle between them. ai.onnx.RotaryEmbedding com.microsoft.GemmaRotaryEmbedding ms com.microsoft.MRotaryEmbedding ms Ranks here for teaching value — position encoding confuses almost everyone, and the rotation picture ends the confusion in ten seconds. Ceiling — the kernel is opset-23, float16 and float32 only. Position identifiers stay logical 64-bit integers.

[33] C5 Transformer internals :: 5 · Three norms, one set of activations The same hidden state goes through layer norm, simplified layer norm and RMS norm. Three histograms update together. You see exactly what removing the mean subtraction costs. ai.onnx.LayerNormalization ai.onnx.SimplifiedLayerNormalization ai.onnx.RMSNormalization com.microsoft.SkipLayerNormalization ms com.microsoft.SkipSimplifiedLayerNormalization ms Ranks here for significance — modern models dropped mean subtraction for speed, and this page shows whether that mattered. Ceiling — the skip variants fold the residual addition inside, so compare them against an explicit add, not against each other.

[34] C5 Transformer internals :: 6 · Fusion audit — do the fused kernels give the same answer Run bias then activation as two calls. Run the fused kernel. Subtract. Show the largest difference and both timings. com.microsoft.BiasGelu ms com.microsoft.FastGelu ms com.microsoft.QuickGelu ms com.microsoft.BiasSplitGelu ms com.microsoft.GemmFastGelu ms ai.onnx.Gelu Ranks here for surprise — FastGelu and QuickGelu are approximations, so the difference is real and measurable, not zero. Ceiling — run-to-run timing variance on this laptop is about 10%. Ignore any gap smaller than that.

[35] C5 Transformer internals :: 7 · Temperature, top-k and the hard maximum One row of logits. Three sliders. You watch the probability distribution flatten and sharpen, and see the point where sampling collapses to a single choice. ai.onnx.Softmax ai.onnx.LogSoftmax ai.onnx.Hardmax ai.onnx.TopK ai.onnx.Div Ranks here for teaching value — every person who uses a language model API sets temperature, and few have seen what it does. Ceiling — small page. Fold it into the decoder-layer project as a panel.

[36] C6 Quantisation and compression :: 1 · Quantisation error microscope One weight matrix, shown five times: full precision, 8-bit, 4-bit, 2-bit, and the error image between each pair. Under each panel sits its size in megabytes. The error picture is where the interest is, because the damage is not spread evenly. ai.onnx.QuantizeLinear ai.onnx.DequantizeLinear ai.onnx.Sub ai.onnx.Abs ai.onnx.ReduceMax Ranks here for visualization and significance — quantisation is normally an abstract claim in a table. Here it is a picture. Ceiling — none worth naming. This is a small, honest, high-value page.

[37] C6 Quantisation and compression :: 2 · The same layer at four bits and at thirty-two Two panels compute an identical linear layer. One holds full weights. One holds four-bit blocks with per-block scales. The outputs match closely and one uses a fraction of the memory. ai.onnx.MatMul com.microsoft.MatMulNBits ms com.microsoft.MatMulBnb4 ms Ranks here for significance — this is the technique that puts a language model on a 4 GB laptop. Ceiling — the manifest supports 2, 4 and 8 bit codes packed low first, with standard unpacked zero points. It does not implement the deprecated group index, prepacked weights or bfloat16.

[38] C6 Quantisation and compression :: 3 · Block size sweep — where quality falls off a cliff A chart with block size on one axis and error on the other. You drag through the power-of-two block sizes and watch the curve bend. com.microsoft.MatMulNBits ms ai.onnx.Sub ai.onnx.ReduceL2 ai.onnx.ReduceMean Ranks here for significance — everyone copies a block size of 32 or 128 from a config file without knowing why. This page answers that. Ceiling — the answer depends on the weight distribution, so use real weights, not a made-up matrix.

[39] C6 Quantisation and compression :: 4 · A vocabulary table that fits in a pocket An embedding table of 32,000 rows, stored quantised, read directly by the gather kernel with no separate dequantisation pass. The page shows the memory before and after. com.microsoft.GatherBlockQuantized ms ai.onnx.Gather ai.onnx.DequantizeLinear Ranks here for surprise — a gather that dequantises inside itself is an unusual kernel and it removes a whole pass. Ceiling — read the card. The block layout must match exactly, or the rows come back as noise.

[40] C6 Quantisation and compression :: 5 · Integer image pipeline against a float one A dropped image runs through a small convolution stack twice: once in float, once in 8-bit integers. The difference map sits between them, amplified so the errors are visible. ai.onnx.QLinearConv ai.onnx.ConvInteger ai.onnx.QLinearMatMul ai.onnx.MatMulInteger ai.onnx.Conv Ranks here for visualization — integer inference is usually discussed as accuracy percentages. Here you see where in the picture it went wrong. Ceiling — picking the right scale and zero point per layer is the real work. Do not expect defaults to look good.

[41] C6 Quantisation and compression :: 6 · Watch the scale get chosen You type numbers into a tensor. The page shows the scale and zero point the dynamic quantiser picks, and how one outlier destroys the resolution of every other value. ai.onnx.DynamicQuantizeLinear ai.onnx.ReduceMin ai.onnx.ReduceMax ai.onnx.Round ai.onnx.Clip Ranks here for teaching value — the outlier problem is the whole reason modern quantisation methods exist, and one typed number demonstrates it. Ceiling — tiny page, big idea. Pair it with the microscope above.

[42] C6 Quantisation and compression :: 7 · The fused quantised blocks of a real model Three kernels that exist only because they appear together in every quantised transformer: the packed query-key-value projection, the packed feed-forward block, and the quantised expert mixture. com.microsoft.MatMulNBitsQkv ms com.microsoft.MatMulNBitsMlp ms com.microsoft.QMoE ms Ranks here for significance — these are the kernels a real quantised model actually calls, so this page is the closest thing to a runtime in the set. Ceiling — highest packing risk in this document. Read all three cards before writing any code.

[43] C7 Attention memory systems :: 1 · Paged attention, drawn as a memory map Blocks of key-value cache appear on screen as coloured rectangles. Each new token claims a block. You start a second conversation and watch it share blocks with the first one instead of copying them. com.microsoft.PagedAttention ms ai.onnx.TensorScatter ai.onnx.Gather Ranks here for significance and surprise — paged attention is the idea that made modern serving cheap, and almost nobody has seen it as a picture. Ceiling — the block table layout is strict. Build the picture from your own book-keeping, not from guesses about the kernel's internals.

[44] C7 Attention memory systems :: 2 · Multi-head against grouped-query against multi-query The same prompt, three head arrangements, three cache sizes on one bar chart. The outputs are almost identical and one cache is eight times smaller than another. ai.onnx.Attention com.microsoft.GroupQueryAttention ms com.microsoft.MultiHeadAttention ms Ranks here for significance — every recent open model uses grouped-query attention, and this page shows why in one chart. Ceiling — the base attention kernel already supports all three modes through its head-count arguments, so use it as the reference and the others as the comparison.

[45] C7 Attention memory systems :: 3 · Sparse attention pattern explorer A large square mask. You drag a window width and a count of global tokens. The mask redraws, and a counter shows how many of the pairs survive. com.microsoft.SparseAttention ms ai.onnx.Trilu ai.onnx.Where ai.onnx.ReduceSum Ranks here for visualization — attention masks are always drawn as diagrams in papers and never as something you can move. Ceiling — the kernel expects a specific block-sparse layout. The picture is easy; matching the layout is the work.

[46] C7 Attention memory systems :: 4 · The quadratic wall, measured on your own card Attention time against sequence length, plotted live from 128 up to as long as the card allows. The curve bends upward and then the page runs out of memory. Both facts are the point. ai.onnx.Attention ai.onnx.MatMul ai.onnx.Softmax Ranks here for significance — this is the constraint behind the whole long-context research field, and here it is your own measurement. Ceiling — 4 GB of video memory ends the curve early. Say where it ended and why.

[47] C7 Attention memory systems :: 5 · Cache eviction, and what forgetting sounds like Generate text with a full cache. Then drop the oldest half, or drop the least-attended entries, and generate again from the same point. The two continuations diverge in front of you. ai.onnx.TensorScatter ai.onnx.TopK ai.onnx.Slice ai.onnx.Attention Ranks here for surprise — eviction policy is normally invisible, and the failure it causes is very legible. Ceiling — needs the whole generation loop from category 5 first.

[48] C7 Attention memory systems :: 6 · Prefill against decode, as two different machines One page, two timings. Processing the prompt saturates the arithmetic units. Producing one token at a time saturates memory bandwidth instead. The chart makes them look like different hardware. ai.onnx.Attention ai.onnx.MatMul com.microsoft.MatMulNBits ms Ranks here for significance — this split explains every serving decision, including why batching helps one phase and not the other. Ceiling — timing variance here is about 10%, so use many repeats.

[49] C8 Mixture of Experts :: 1 · Routing map — watch tokens pick their experts You type a sentence. Each word is coloured by the expert that received it. Type a line of code and the colours change completely. com.microsoft.MoE ms ai.onnx.Softmax ai.onnx.TopK ai.onnx.OneHot Ranks here for visualization and surprise — routing is the defining property of the largest models in the world and it is almost never shown. Ceiling — needs real expert weights. Without them the colours are noise.

[50] C8 Mixture of Experts :: 2 · Do experts specialise? Test it yourself Feed four documents: prose, source code, mathematics, another language. Four routing histograms appear. If specialisation is real, the histograms differ visibly. com.microsoft.MoE ms ai.onnx.ReduceSum ai.onnx.ArgMax ai.onnx.Div Ranks here for significance — this is a genuine open research question, and the page is an experiment rather than a demonstration. Ceiling — the honest result may be that specialisation is weak. Report what you measure.

[51] C8 Mixture of Experts :: 3 · Load collapse, live A bar per expert, updating as tokens flow. Turn the balancing term off and watch a few bars grow while the rest starve. com.microsoft.MoE ms ai.onnx.ReduceSum ai.onnx.ScatterND ai.onnx.Softmax Ranks here for surprise — the failure mode is dramatic and fast, and it explains an entire branch of training-loss design. Ceiling — you are visualising a routing decision, not training, so the collapse must be set up rather than learned.

[52] C8 Mixture of Experts :: 4 · How many experts per token is enough A slider for the top-k value. Output quality on one side, arithmetic cost on the other. The curve flattens sooner than most people expect. com.microsoft.MoE ms ai.onnx.TopK ai.onnx.SwiGLU ai.onnx.MatMul Ranks here for significance — the choice between one and two experts per token is a real engineering decision with a visible answer. Ceiling — the kernel supports several gate fusion layouts. Pick one and stay with it.

[53] C8 Mixture of Experts :: 5 · The parameter count that is never used Two numbers, side by side: total parameters and parameters touched by this token. The gap is the entire argument for the architecture. com.microsoft.MoE ms ai.onnx.ReduceSum ai.onnx.Mul Ranks here for teaching value — one screen replaces a long explanation of sparse models. Ceiling — a counter, not a computation. Put it inside project 1 rather than alone.

[54] C8 Mixture of Experts :: 6 · Quantised experts against full-precision experts The same mixture layer, run twice, with the difference and the memory saving both shown. com.microsoft.QMoE ms com.microsoft.MoE ms ai.onnx.Sub ai.onnx.ReduceL2 Ranks here for significance — quantised experts are how a sparse model fits on small hardware. Ceiling — two strict packing conventions at once. This is the hardest single build in the document.

[55] C9 Sequences without transformers :: 1 · A state-space block, running in a tab Linear attention plus a causal convolution that carries state. Cost grows with sequence length, not with its square. A chart against the attention curve from category 7 makes the difference obvious. ai.onnx.LinearAttention ai.onnx.CausalConvWithState com.microsoft.LinearAttentionGate ms com.microsoft.GatedAdd ms Ranks here for surprise — Mamba-style architectures are the main alternative to attention and nobody expects them in a browser. Ceiling — the least documented kernels in the set. Read every card, and expect to spend the time on layout, not on maths.

[56] C9 Sequences without transformers :: 2 · Look inside an LSTM while it reads Feed a character sequence. Three gate values per cell, drawn as bars that move with each character. You can see a forget gate close at the end of a word. ai.onnx.LSTM ai.onnx.Sigmoid ai.onnx.Tanh Ranks here for visualization — the gate picture is the classic diagram, and here it moves with real data. Ceiling — the kernel returns the sequence and the final state. Intermediate gate values may need a hand-wired cell to expose.

[57] C9 Sequences without transformers :: 3 · Three recurrent cells on one problem A plain recurrent net, a gated unit and a long short-term memory, all reading the same long sequence. Only two of them still remember the first character at the end. ai.onnx.RNN ai.onnx.GRU ai.onnx.LSTM ai.onnx.Tanh Ranks here for significance — the vanishing memory problem is why gates were invented, and this shows it rather than asserting it. Ceiling — without training this is a demonstration of dynamics, not of accuracy.

[58] C9 Sequences without transformers :: 4 · Streaming state, proven identical to batch Run a long signal in one pass. Then run it one chunk at a time, carrying the state across chunks. Subtract the two outputs. The difference should be zero. ai.onnx.CausalConvWithState com.microsoft.VarlenCausalConvWithState ms ai.onnx.Sub ai.onnx.ReduceMax Ranks here for significance — this equivalence is the whole basis of real-time inference, and proving it is more convincing than reading it. Ceiling — an off-by-one in the state handover shows up as a small non-zero difference. That is the test working.

[59] C9 Sequences without transformers :: 5 · Ragged batches without waste Ten sequences of different lengths, packed end to end instead of padded to the longest. A bar chart shows the wasted computation you avoided. com.microsoft.VarlenCausalConvWithState ms ai.onnx.CumSum ai.onnx.Gather ai.onnx.Compress Ranks here for significance — padding waste is a large hidden cost in real serving. Ceiling — offset book-keeping is fiddly. The cumulative sum kernel builds the offsets for you.

[60] C9 Sequences without transformers :: 6 · Reverse a batch of padded sequences correctly Two panels reverse the same padded batch. One uses a plain flip and drags the padding to the front. The other uses the length-aware kernel and does not. The bug is visible. ai.onnx.ReverseSequence ai.onnx.Slice ai.onnx.Where Ranks here for teaching value — this is a real bug that people ship, and one screenshot explains it forever. Ceiling — small. A panel, not a page.

[61] C9 Sequences without transformers :: 7 · A loop that lives in the graph The same iteration written twice: once as a JavaScript loop over kernel calls, once as a single graph-level loop operation. Both give the same answer. ai.onnx.Loop ai.onnx.Scan ai.onnx.If ai.onnx.Identity Ranks here for surprise — most people never learn that control flow is itself an operator. Ceiling — these are control-flow operators, so check what the WebGPU package actually implements before designing around them.

[62] C10 Embeddings, retrieval and search :: 1 · Search your own notes, entirely on your machine hybrid Drop a folder of Markdown. The page embeds it once, keeps the vectors on the GPU, and answers a typed question by meaning rather than by keyword. No server, no key, nothing sent anywhere. ai.onnx.MatMul ai.onnx.LpNormalization ai.onnx.TopK ai.onnx.Gather Ranks here for significance — it is the one project here you would keep using after building it. Ceiling — embedding is the slow part and it runs through Transformers.js. Cache the vectors in the browser so the second visit is instant.

[63] C10 Embeddings, retrieval and search :: 2 · Embedding atlas — ten thousand documents as a map hybrid A scatter plot where related documents sit together. You hover a point and read the text. You lasso a region and read the whole cluster. ai.onnx.MatMul ai.onnx.ReduceMean ai.onnx.Sub ai.onnx.LpNormalization ai.onnx.TopK Ranks here for visualization — a good embedding map is the most persuasive object in machine learning. Ceiling — there is no decomposition kernel, so reduce dimensions by power iteration or by random projection. Both are matrix multiplies.

[64] C10 Embeddings, retrieval and search :: 3 · Near-duplicate hunter over a large text set hybrid Ten thousand documents in, a list of suspiciously similar pairs out, with the two texts side by side. The full similarity matrix is one large matrix multiply. ai.onnx.MatMul ai.onnx.Greater ai.onnx.NonZero ai.onnx.Trilu ai.onnx.TopK Ranks here for significance — training-set contamination and duplicate content are real problems and this is a real tool. Ceiling — a 10,000-square matrix is 100 million values. Process it in blocks and keep each block on the GPU.

[65] C10 Embeddings, retrieval and search :: 4 · A retrieval-augmented answer, fully local hybrid Ask a question. The page retrieves passages with kernels and writes the answer with a small local model. The retrieved passages stay on screen next to the answer. ai.onnx.MatMul ai.onnx.TopK ai.onnx.LpNormalization ai.onnx.Softmax Ranks here for significance — the entire private-assistant pattern, with nothing leaving the laptop. Ceiling — 4 GB of video memory caps the generator. Keep it near 500M parameters and expect modest writing.

[66] C10 Embeddings, retrieval and search :: 5 · Compress the vectors and measure what you lost The same search, run against full vectors and against quantised ones. Two result lists, a recall number, and a memory number. ai.onnx.QuantizeLinear ai.onnx.MatMulInteger ai.onnx.TopK ai.onnx.Equal ai.onnx.ReduceSum Ranks here for significance — every vector database makes this trade and hides the number. Here it is measured. Ceiling — proper product quantisation needs clustering first, which is category 11.

[67] C10 Embeddings, retrieval and search :: 6 · Five languages, one meaning hybrid The same sentence in five languages, embedded by a multilingual model, plotted together. The five points sit almost on top of each other. An unrelated sentence sits far away. ai.onnx.MatMul ai.onnx.LpNormalization ai.onnx.Sub ai.onnx.ReduceL2 Ranks here for surprise — cross-language alignment is a strange and beautiful property, and one picture carries it. Ceiling — small page, and it depends entirely on the model you pick.

[68] C10 Embeddings, retrieval and search :: 7 · Re-rank the shortlist and watch the order change hybrid Twenty results from a fast search, re-scored by a slower and more careful model. Arrows show which results moved and how far. ai.onnx.MatMul ai.onnx.TopK ai.onnx.Softmax ai.onnx.Gather Ranks here for teaching value — two-stage retrieval is standard practice and the movement arrows explain it instantly. Ceiling — the second model does the work. The kernels only order the list.

[69] C11 Classical machine learning and statistics :: 1 · k-means on a million points, converging in front of you A million points on screen. Centres jump, colours flip, and the whole picture settles after a few seconds. You drag a centre and watch the clustering recover. ai.onnx.MatMul ai.onnx.ReduceSumSquare ai.onnx.ArgMin ai.onnx.OneHot ai.onnx.Div Ranks here for visualization — convergence is a process, and almost every explanation of it is a still image. Ceiling — the assignment step is a distance matrix, so memory grows with points multiplied by clusters. Block it.

[70] C11 Classical machine learning and statistics :: 2 · Principal components without a decomposition kernel Drop a spreadsheet. The page centres the data, then finds the leading directions by repeated multiplication, and plots the first two. A counter shows the iterations to convergence. ai.onnx.MatMul ai.onnx.ReduceMean ai.onnx.Sub ai.onnx.LpNormalization ai.onnx.Gemm Ranks here for surprise — there is no SVD in the 207, so the standard route is closed and power iteration works anyway. Ceiling — close eigenvalues converge slowly. Show the iteration count so the slowness is visible rather than mysterious.

[71] C11 Classical machine learning and statistics :: 3 · Correlation heat map for any dropped spreadsheet Drop a CSV with a thousand columns. A full correlation matrix appears at once, sorted so that related columns sit together. ai.onnx.ReduceMean ai.onnx.Sub ai.onnx.MatMul ai.onnx.Sqrt ai.onnx.Div Ranks here for significance — this is a genuinely useful data tool that normally needs a Python environment. Ceiling — sorting the rows and columns is a JavaScript step, because there is no sort kernel.

[72] C11 Classical machine learning and statistics :: 4 · Soft clustering with expectation and maximisation The same points as project 1, but each one is partly in several clusters. Colours blend at the boundaries, and ellipses show the fitted shapes. ai.onnx.Exp ai.onnx.Softmax ai.onnx.MatMul ai.onnx.ReduceSum ai.onnx.Log Ranks here for visualization — blended membership is the clearest possible picture of the difference between hard and soft assignment. Ceiling — full covariance needs a matrix inverse, which does not exist here. Use diagonal covariance.

[73] C11 Classical machine learning and statistics :: 5 · Nearest neighbours you can draw into Click to place labelled points. The decision surface redraws immediately for every pixel of the canvas. Change the neighbour count and watch the boundary get smoother. ai.onnx.MatMul ai.onnx.TopK ai.onnx.Gather ai.onnx.OneHot ai.onnx.ArgMax Ranks here for visualization — classifying every pixel is expensive on a CPU and free here, which is what makes the interaction feel instant. Ceiling — none. This is a clean small build.

[74] C11 Classical machine learning and statistics :: 6 · Density estimation as a landscape Scattered points become a smooth surface. A bandwidth slider takes it from spiky to featureless, and the honest answer sits in between. ai.onnx.Sub ai.onnx.Exp ai.onnx.ReduceSum ai.onnx.Conv ai.onnx.Div Ranks here for teaching value — bandwidth choice is the entire subject and the slider teaches it in five seconds. Ceiling — a Gaussian smoothing convolution is far cheaper than all-pairs distances. Use the convolution route for large sets.

[75] C11 Classical machine learning and statistics :: 7 · Monte Carlo estimation, with the awkward part named Estimate an integral, or an option price, from millions of samples. A running estimate narrows toward the true value as the sample count grows. ai.onnx.CumSum ai.onnx.CumProd ai.onnx.Exp ai.onnx.ReduceMean ai.onnx.Max Ranks here for significance — path simulation is exactly what wide parallel hardware is for. Ceiling — the missing random number generator hurts most here. Generate the sample block in JavaScript, upload it once, and re-use it with different offsets.

[76] C12 Training in the browser :: 1 · Train a digit classifier in the tab, then draw a digit Drop the training set. A loss curve falls. When it flattens, you draw a number with the mouse and the page reads it. Nothing was pre-trained and nothing was downloaded. ai.onnx.MatMul ai.onnx.Transpose ai.onnx.Softmax ai.onnx.Sub ai.onnx.Mul ai.onnx.ReduceMean Ranks here for all three axes — training is the last thing anyone expects from this library, the result is interactive, and the gradient of softmax with cross-entropy is a subtraction. Ceiling — a linear classifier reaches about 92% and a small hidden layer does better. Shuffling happens in JavaScript.

[77] C12 Training in the browser :: 2 · Two moons, one hidden layer, gradients by hand Two interleaved crescents that no straight line can separate. The decision surface bends across the canvas as training runs, and finally wraps around both shapes. ai.onnx.MatMul ai.onnx.Transpose ai.onnx.Relu ai.onnx.Sigmoid ai.onnx.Greater ai.onnx.Where Ranks here for visualization — the moving boundary is the single best picture of what learning is. Ceiling — the gradient of the rectifier is a comparison and a select, so the backward pass costs two more calls than people expect.

[78] C12 Training in the browser :: 3 · The loss landscape, as a surface you can fly over Pick two directions in parameter space. Evaluate the loss on a grid across them. A valley appears, and the path taken by training is drawn on top of it. ai.onnx.MatMul ai.onnx.ReduceMean ai.onnx.Log ai.onnx.Add ai.onnx.Mul Ranks here for surprise — this is a famous set of pictures from a research paper, and the only reason it is rare is that it costs thousands of forward passes. On a GPU that is a grid evaluation. Ceiling — the surface is a two-dimensional slice of a very high-dimensional space. Say so on the page.

[79] C12 Training in the browser :: 4 · Teach a real model a new class in thirty seconds hybrid Give five examples of two new categories. The page embeds them with a real model and trains only a final linear layer. It then classifies anything you drop. ai.onnx.MatMul ai.onnx.Transpose ai.onnx.Softmax ai.onnx.Sub ai.onnx.LpNormalization Ranks here for significance — a linear probe on frozen features is how most practical custom classifiers are actually built. Ceiling — only the last layer trains. The frozen model decides the accuracy ceiling.

[80] C12 Training in the browser :: 5 · Optimiser race on one chart Plain descent, descent with momentum, and an adaptive method, all written by hand, all training the same network. Three loss curves race. One of them oscillates and you can see why. ai.onnx.Mul ai.onnx.Add ai.onnx.Sqrt ai.onnx.Div ai.onnx.Pow Ranks here for teaching value — optimisers are usually a string in a config file, and here they are five lines of arithmetic each. Ceiling — keep the optimiser state on the GPU or the update becomes the slowest part of the loop.

[81] C12 Training in the browser :: 6 · Poison the training set with a click A working classifier. You add one badly labelled point and press train. The boundary lurches. You add ten and it collapses. ai.onnx.MatMul ai.onnx.Sigmoid ai.onnx.Sub ai.onnx.Mul ai.onnx.ReduceMean Ranks here for significance — data quality beats model choice, and this makes that lesson physical. Ceiling — logistic regression is fragile by design here. That is the point, not a flaw.

[82] C12 Training in the browser :: 7 · Watch a model start to memorise Two curves from the same run. Training loss keeps falling. Held-out loss turns upward. The page marks the exact step where they part. ai.onnx.MatMul ai.onnx.ReduceMean ai.onnx.Sub ai.onnx.Mul ai.onnx.Relu Ranks here for teaching value — overfitting is defined by that crossing point and most people have only read about it. Ceiling — you must hold out data honestly. Split before training, not after.

[83] C13 Iterative linear algebra and graphs :: 1 · PageRank, flowing across a real graph Load a link graph. Every node starts equal. Rank flows along the edges, iteration by iteration, and the node sizes change on screen until the picture stops moving. ai.onnx.MatMul ai.onnx.LpNormalization ai.onnx.Add ai.onnx.Mul ai.onnx.ReduceSum Ranks here for visualization and significance — the algorithm that built a search company is repeated multiplication, and watching it settle makes that concrete. Ceiling — a dense matrix limits you to a few thousand nodes. A sparse graph needs gather-based multiplication instead.

[84] C13 Iterative linear algebra and graphs :: 2 · Solve a linear system with no solver Set up a system, press run, and watch the residual fall on a log chart. Conjugate gradient reaches the answer in a few dozen steps using only multiplications and dot products. ai.onnx.MatMul ai.onnx.ReduceSum ai.onnx.Mul ai.onnx.Sub ai.onnx.Div Ranks here for surprise — the obvious route is closed, and the professional route was iterative all along. Direct inversion is rare in real numerical work. Ceiling — the method needs a symmetric positive-definite matrix. For anything else, use the least-squares form.

[85] C13 Iterative linear algebra and graphs :: 3 · Split a network in two along its natural seam A graph with no obvious structure. The page computes the second-smallest eigenvector of its Laplacian by iteration, colours the nodes by sign, and the two communities separate cleanly. ai.onnx.MatMul ai.onnx.Sub ai.onnx.LpNormalization ai.onnx.ReduceSum ai.onnx.Sign Ranks here for surprise — spectral clustering feels like magic the first time a graph falls apart along the right line. Ceiling — you must remove the constant vector at every step or the iteration converges to the wrong thing.

[86] C13 Iterative linear algebra and graphs :: 4 · Influence spreading through a network Click a node. Heat spreads outward along the edges over time, and the page shows who is genuinely close rather than who is one hop away. ai.onnx.MatMul ai.onnx.Exp ai.onnx.Mul ai.onnx.Add ai.onnx.Div Ranks here for visualization — graph distance and graph influence are different things, and this shows the difference moving. Ceiling — the matrix exponential is approximated by scaling and squaring, which is a short series of multiplications.

[87] C13 Iterative linear algebra and graphs :: 5 · Where does a Markov chain settle Build a transition table by clicking. The distribution marches to its steady state and stops. Change one probability and watch the whole equilibrium move. ai.onnx.MatMul ai.onnx.Div ai.onnx.ReduceSum ai.onnx.CumProd Ranks here for teaching value — steady state is an abstract term until you see the numbers stop changing. Ceiling — a chain that is not connected has several steady states. Let the user build one and find out.

[88] C13 Iterative linear algebra and graphs :: 6 · Why some systems are hard Two systems that look alike. One converges in twelve steps. The other needs eight hundred. The page draws both error curves and the two matrices next to them. ai.onnx.EyeLike ai.onnx.Trilu ai.onnx.MatMul ai.onnx.ReduceL2 ai.onnx.Div Ranks here for significance — conditioning is the reason numerical code fails, and it is usually taught with no picture at all. Ceiling — you cannot compute the condition number exactly without a decomposition. Estimate it from the iteration count.

[89] C14 General GPU primitives and data work :: 1 · A data table on the GPU: filter, group and join a million rows Drop a CSV. Filter it with a condition, group it by a column, join it against a second table, and see the timings for each step. No database and no server. ai.onnx.Greater ai.onnx.Compress ai.onnx.NonZero ai.onnx.Unique ai.onnx.ScatterND ai.onnx.GatherND ai.onnx.CumSum Ranks here for surprise and significance — a kernel library published for machine learning turns out to contain a small query engine. Ceiling — no sort kernel, so ordered output falls back to JavaScript. Grouping works because the unique kernel exists.

[90] C14 General GPU primitives and data work :: 2 · Take a floating point number apart Type a number. See its sign bit, its exponent and its fraction, live. Then watch what the bits look like at the moment a computation produces infinity or a non-number. ai.onnx.BitCast ai.onnx.BitShift ai.onnx.BitwiseAnd ai.onnx.IsNaN ai.onnx.IsInf Ranks here for surprise — BitCast lets you look at a float as an integer, which is the trick behind a generation of fast approximate maths. Ceiling — a small page, but the one that removes the most confusion per line of code.

[91] C14 General GPU primitives and data work :: 3 · The scan, and everything it builds One prefix sum drives four demonstrations on the same page: stream compaction, run-length encoding, a histogram, and allocating variable-sized output. ai.onnx.CumSum ai.onnx.Where ai.onnx.ScatterND ai.onnx.Equal ai.onnx.Range Ranks here for teaching value — the prefix sum is the most important parallel primitive and the least intuitive, and four uses on one page settle it. Ceiling — none. The kernel already handles exclusive and reversed scans.

[92] C14 General GPU primitives and data work :: 4 · Sparse matrix multiplication with no sparse kernel A sparse matrix stored as indices and values, multiplied by a dense vector using gather and scatter, timed against the dense version of the same matrix. ai.onnx.GatherElements ai.onnx.ScatterElements ai.onnx.Mul ai.onnx.ReduceSum ai.onnx.CumSum Ranks here for significance — it unlocks the large graphs that category 13 cannot reach with dense matrices. Ceiling — scatter with repeated indices has an ordering issue. Use segmented reduction over sorted indices instead.

[93] C14 General GPU primitives and data work :: 5 · Deduplicate and compare large sets Two lists of a million values. Unique values, shared values, and values in one list only, all computed on the card and reported with counts. ai.onnx.Unique ai.onnx.Equal ai.onnx.ReduceMax ai.onnx.Compress ai.onnx.NonZero Ranks here for significance — set operations at this size are slow in JavaScript and immediate here. Ceiling — membership testing against a large set is an all-pairs comparison unless you block it.

[94] C14 General GPU primitives and data work :: 6 · Bit-trick sandbox Hashing, bit masks, cyclic shifts and ordered dithering of a dropped image, all done with integer operations and no arithmetic on floats. ai.onnx.BitwiseXor ai.onnx.BitwiseAnd ai.onnx.BitwiseOr ai.onnx.BitwiseNot ai.onnx.BitShift ai.onnx.Mod Ranks here for surprise — a set of kernels published for neural networks includes a complete integer toolkit. It is also a way to make repeatable pseudo-random values without a generator kernel. Ceiling — a hash written this way is for dithering and bucketing, not for security.

[95] C14 General GPU primitives and data work :: 7 · Reorder a million rows without a sort Shuffle, partition and permute large arrays using index arithmetic alone, with a chart of how long each pattern takes. ai.onnx.Range ai.onnx.Gather ai.onnx.Mod ai.onnx.Where ai.onnx.ConstantOfShape Ranks here for teaching value — it makes the missing sort kernel concrete, and shows what you can still do without it. Ceiling — the permutation itself comes from JavaScript, for the same reason as everywhere else.

[96] C15 Vision heads, wired by hand :: 1 · Build the detection head yourself hybrid A model produces raw boxes and scores. Your page decodes them, filters them, suppresses the overlapping ones, and draws the survivors. You wrote the part that turns a tensor into a picture with boxes on it. ai.onnx.NonMaxSuppression ai.onnx.TopK ai.onnx.Greater ai.onnx.Gather ai.onnx.Concat Ranks here for significance — the head is the part everyone imports and nobody reads, and it is four kernels. Ceiling — box coordinate conventions differ between models. Getting the format right is most of the work.

[97] C15 Vision heads, wired by hand :: 2 · Overlap suppression, with a slider Five hundred overlapping boxes on one photograph. Drag the overlap threshold and watch boxes merge into one, then split apart again. A counter shows how many survive. ai.onnx.NonMaxSuppression ai.onnx.TopK ai.onnx.Where ai.onnx.ReduceMax Ranks here for visualization — a threshold that people copy from a config file becomes something they can feel. Ceiling — none. This is a clean small build and a good first use of the kernel.

[98] C15 Vision heads, wired by hand :: 3 · Deformable sampling, drawn on the image A convolution whose sampling points move. The page draws the sampling grid on top of the picture, and you drag the offsets and watch the grid wrap around an object instead of sitting square. ai.onnx.DeformConv ai.onnx.Conv ai.onnx.GridSample ai.onnx.Sub Ranks here for surprise — deformable convolution is one of the least-visualised ideas in computer vision and one of the more beautiful. Ceiling — real offsets are learned. Hand-set offsets show the mechanism, not the accuracy.

[99] C15 Vision heads, wired by hand :: 4 · Why aligned pooling replaced rounded pooling One region of interest, two croppings. The old method rounds the box to whole pixels. The aligned method samples between them. The page shows both grids and the half-pixel shift between the results. ai.onnx.RoiAlign ai.onnx.MaxPool ai.onnx.Resize ai.onnx.Sub Ranks here for teaching value — a half-pixel error sounds trivial and destroyed segmentation accuracy for years. Ceiling — the effect is subtle. Zoom in hard or the picture proves nothing.

[100] C15 Vision heads, wired by hand :: 5 · Clean up a rough segmentation mask hybrid A blocky mask from a model, refined against the image edges. Holes close, edges snap to the object, and a slider controls how far the refinement goes. ai.onnx.Conv ai.onnx.Resize ai.onnx.Where ai.onnx.Greater ai.onnx.MaxPool Ranks here for visualization — mask refinement is a large visible improvement from very little arithmetic. Ceiling — this is guided smoothing, not a trained refinement network. Do not claim more.

[101] C15 Vision heads, wired by hand :: 6 · Anchor boxes, all of them, at once Every anchor a detector considers, drawn over the picture. Thousands of faint rectangles at several scales. It explains the design of a detector faster than any diagram. ai.onnx.Range ai.onnx.Tile ai.onnx.Concat ai.onnx.Mul ai.onnx.Add Ranks here for teaching value — seeing the sheer number of candidate boxes explains why suppression exists. Ceiling — pure geometry. No model needed, which is also its charm.

[102] C16 Teaching and numerical toys :: 1 · The activation zoo, all twenty of them Every activation function in the collection, on one plot, with its derivative below it. Toggle any of them. The dead region of the rectifier, the smooth tail of the error-function variant, and the negative dip of the self-gated ones all appear together. ai.onnx.Relu ai.onnx.LeakyRelu ai.onnx.PRelu ai.onnx.Elu ai.onnx.Celu ai.onnx.Selu ai.onnx.Gelu ai.onnx.Mish ai.onnx.Swish ai.onnx.HardSwish ai.onnx.HardSigmoid ai.onnx.Softplus ai.onnx.Softsign ai.onnx.Shrink ai.onnx.ThresholdedRelu ai.onnx.Erf ai.onnx.Sigmoid ai.onnx.Tanh com.microsoft.FastGelu ms com.microsoft.QuickGelu ms Ranks here for significance — twenty kernels in one page, and a reference you would keep open while reading papers. Ceiling — none. It is the highest coverage per line of code in this document.

[103] C16 Teaching and numerical toys :: 2 · Einstein summation playground Type a subscript string into a box. The page runs it on the GPU and shows the result, the shapes and the time. Matrix multiply, trace, transpose, outer product and batched attention are all preset buttons. ai.onnx.Einsum ai.onnx.MatMul ai.onnx.Transpose Ranks here for surprise — it is a GPU tensor calculator with no installation, and it is the smallest useful thing on this whole list. Ceiling — the manifest supports full equation strings including implicit output terms. Malformed strings need a clear error message.

[104] C16 Teaching and numerical toys :: 3 · Why the stable form of softmax exists Two panels compute the same thing. One subtracts the maximum first. As you raise the input values, the naive panel turns into non-numbers and the stable one does not. Indicator lights show which values broke. ai.onnx.Exp ai.onnx.ReduceLogSumExp ai.onnx.ReduceMax ai.onnx.IsNaN ai.onnx.IsInf ai.onnx.Softmax Ranks here for significance — this trick is in every framework and few people have seen the failure it prevents. Ceiling — a single-screen page with one very clear message.

[105] C16 Teaching and numerical toys :: 4 · Half precision, and where it breaks The same computation in 32-bit and 16-bit floats, with the growing difference plotted as the numbers get larger or smaller. Your Chrome reports half-precision support as available, so both run. ai.onnx.Cast ai.onnx.CastLike ai.onnx.Sub ai.onnx.Abs ai.onnx.ReduceMax ai.onnx.IsInf Ranks here for significance — half precision is the default in modern inference and its limits are usually invisible until something breaks. Ceiling — a kernel may not implement every type. Check each manifest before assuming.

[106] C16 Teaching and numerical toys :: 5 · The complete reduction gallery One tensor, eleven reductions, one screen. Sum, mean, maximum, minimum, product, both norms, sum of squares, and the logarithmic variants, each with its axis and its result. ai.onnx.ReduceSum ai.onnx.ReduceMean ai.onnx.ReduceMax ai.onnx.ReduceMin ai.onnx.ReduceProd ai.onnx.ReduceL1 ai.onnx.ReduceL2 ai.onnx.ReduceSumSquare ai.onnx.ReduceLogSum ai.onnx.ReduceLogSumExp ai.onnx.ArgMax Ranks here for teaching value — axis and keep-dimension arguments confuse everyone, and eleven live examples end the confusion. Ceiling — a reference card, not a discovery.

[107] C16 Teaching and numerical toys :: 6 · Broadcasting, drawn as blocks Two shapes you can edit. The page draws how they line up, colours the dimensions that stretch, and refuses the combinations that are illegal. ai.onnx.Expand ai.onnx.Tile ai.onnx.Add ai.onnx.Mul ai.onnx.Shape Ranks here for teaching value — broadcasting errors are the most common failure in tensor code. Ceiling — remember that some kernels refuse to broadcast at all. The gated activation is one of them.

[108] C16 Teaching and numerical toys :: 7 · Does the GPU agree with JavaScript Trigonometric, hyperbolic and error functions computed both ways across a wide range, with the difference plotted. Mostly a flat line, with interesting bumps at the extremes. ai.onnx.Sin ai.onnx.Cos ai.onnx.Tan ai.onnx.Sinh ai.onnx.Cosh ai.onnx.Asin ai.onnx.Acos ai.onnx.Atan ai.onnx.Asinh ai.onnx.Acosh ai.onnx.Atanh ai.onnx.Erf Ranks here for surprise — twelve kernels tested at once, and the answer is not always identical. Ceiling — small differences are expected and correct. Report them as differences, not as bugs.

[109] C17 Hardware forensics :: 1 · Two graphics cards, one page Run the same workload on the Intel chip and on the discrete card, and put the two results side by side with the adapter names. The page also shows the exact Windows setting that chooses the card, because the browser preference alone does not. ai.onnx.MatMul ai.onnx.Conv ai.onnx.Softmax Ranks here for significance — you already proved that a browser preference cannot switch cards on this laptop, and that finding is worth publishing as a page. Ceiling — Chrome binds one adapter when its process starts. You must quit Chrome completely between the two runs, so the page compares saved results.

[110] C17 Hardware forensics :: 2 · Which shader variant did it choose, and why The matrix multiply kernel ships fourteen shader templates. The page runs a range of shapes, reports the variant chosen for each one, and shows which conditions in the manifest decided it. ai.onnx.MatMul com.microsoft.FusedMatMul ms ai.onnx.Gemm com.microsoft.FusedGemm ms Ranks here for surprise — dispatch selection is invisible in every other GPU stack, and here it is data you can read. On this laptop the matrix-unit variant is never chosen, because Chrome reports that feature as absent on both adapters. Ceiling — you are reading a manifest and inferring. Say that plainly.

[111] C17 Hardware forensics :: 3 · The cost of coming back to JavaScript The same operation at eight sizes, run twice: once returning to JavaScript, once staying on the card. Two curves, and the gap between them is the whole lesson. ai.onnx.MatMul ai.onnx.Add ai.onnx.Mul Ranks here for significance — you already measured 26 ms against 9.3 ms at 1024 squared on this card. A curve turns one measurement into a rule. Ceiling — run-to-run variance is about 10%. Repeat every point and show the spread.

[112] C17 Hardware forensics :: 4 · Find the corner of your card's performance Plot achieved throughput against the arithmetic done per byte moved. Small operations sit on the bandwidth slope. Large matrix multiplies sit on the flat arithmetic ceiling. The bend is your machine's balance point. ai.onnx.MatMul ai.onnx.Add ai.onnx.Gemm ai.onnx.ReduceSum Ranks here for significance — one chart predicts whether any future kernel chain will be fast, which no single benchmark can. Ceiling — the roofline needs the card's peak numbers as a reference line. Measure them, do not quote a specification sheet.

[113] C17 Hardware forensics :: 5 · Watch the laptop get slower as it warms Run the same workload every second for ten minutes. Plot the result. On a laptop the line usually starts flat and then steps down, and it does not come back until the fans catch up. ai.onnx.MatMul ai.onnx.Conv Ranks here for surprise — every benchmark you will ever read was measured in the first thirty seconds, and this shows what happens after. Ceiling — the browser cannot read temperature. You are inferring heat from throughput, so label the chart honestly.

[114] C17 Hardware forensics :: 6 · Full census of what your adapters support Every feature and every limit of both adapters, side by side, with the values that matter for these kernels marked. A copy button produces a report you can paste into an issue. ai.onnx.Identity ai.onnx.Cast Ranks here for teaching value — it is the page you open first whenever anything behaves differently on another machine. Ceiling — mostly browser interrogation rather than kernel work. Useful anyway.

[115] C18 Games and interactive toys :: 1 · Slime mould — a million agents that build networks Particles move, leave a trail, and steer toward trails ahead of them. Within seconds they organise into veins and webs that look like a living organism. It is the best-looking thing you can build from these kernels. ai.onnx.GridSample ai.onnx.Conv ai.onnx.ScatterND ai.onnx.Sin ai.onnx.Cos ai.onnx.Mul Ranks here for visualization and surprise — sensing ahead of each agent is a sampling call, and trail decay is a blur. Two kernels produce emergent behaviour. Ceiling — turning decisions need a random component, which comes from a pre-uploaded noise buffer.

[116] C18 Games and interactive toys :: 2 · Falling sand Sand, water, oil and fire in a box. You draw with the mouse. Sand piles, water levels itself, fire climbs. Every cell updates at once. ai.onnx.Conv ai.onnx.Where ai.onnx.Equal ai.onnx.Greater ai.onnx.Max Ranks here for visualization — it is the most playable thing here, and the rules are pure neighbour comparison. Ceiling — a parallel update can move the same grain twice. Use a checkerboard update order, which is the standard fix.

[117] C18 Games and interactive toys :: 3 · Life, at sixteen million cells A four-thousand square grid of Conway's rules, running at full frame rate, with zoom and pan. Gliders cross the whole screen in a few seconds. ai.onnx.Conv ai.onnx.Equal ai.onnx.Or ai.onnx.And ai.onnx.Where Ranks here for visualization — the neighbour count is a 3×3 convolution and the rule is two comparisons. Scale is what makes it new. Ceiling — none worth naming. This is the cheapest impressive build in the document.

[118] C18 Games and interactive toys :: 4 · Flocking, ten thousand birds Separation, alignment and cohesion over ten thousand agents. Flocks split around obstacles you place and rejoin behind them. ai.onnx.Sub ai.onnx.MatMul ai.onnx.Less ai.onnx.Where ai.onnx.ReduceSum ai.onnx.LpNormalization Ranks here for visualization — flocking is normally limited by the neighbour search, and all-pairs distance is a matrix multiply here. Ceiling — all-pairs grows with the square of the count. Ten thousand agents is a hundred million pairs, which is near the limit for a smooth frame rate.

[119] C18 Games and interactive toys :: 5 · Watch a maze get solved by a spreading wave A flood front expands from the start, follows every corridor at once, and reaches the exit. The shortest path is then traced backward down the arrival times. ai.onnx.Conv ai.onnx.MaxPool ai.onnx.Where ai.onnx.Min ai.onnx.Add Ranks here for surprise — a breadth-first search is a queue on a processor and a repeated dilation on a GPU, and the second version is far prettier. Ceiling — the number of iterations equals the path length, so a big maze is many small dispatches.

[120] C18 Games and interactive toys :: 6 · Ant colony pathfinding Agents wander, drop pheromone on the way back, and a shortest route emerges from the fog after a minute. Move the food and watch the trail migrate. ai.onnx.GridSample ai.onnx.ScatterND ai.onnx.Conv ai.onnx.Mul ai.onnx.Clip Ranks here for significance — it is a real optimisation method and it looks like a nature documentary. Ceiling — shares the slime mould machinery. Build one, then the other is an afternoon.

[121] C18 Games and interactive toys :: 7 · Solve a light-switch puzzle with integer algebra A grid of lights. Pressing one flips its neighbours. The page solves the whole board at once by elimination over two-valued arithmetic and shows the winning presses. ai.onnx.BitwiseXor ai.onnx.Xor ai.onnx.MatMul ai.onnx.Mod ai.onnx.Where Ranks here for surprise — a puzzle turns out to be a linear system over a two-element field, and the bitwise kernels handle it directly. Ceiling — elimination is sequential by row, so this is many small dispatches on a small matrix.